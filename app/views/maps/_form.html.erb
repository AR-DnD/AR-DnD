
<%= form_with(model: map, local: true) do |form| %>
  <% if map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(map.errors.count, "error") %> prohibited this map from being saved:</h2>

      <ul>
      <% map.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :map_name %>
  </div>

  <div class="field">
    <%= form.label :data %>
    <%= form.text_field :data, id: :map_data %>
  </div>

  <div class="field">
    <%= form.label :story %>
    <%= form.text_field :story, id: :map_story %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
