<%= form_with(model: map, local: true) do |form| %>
  <% if map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(map.errors.count, "error") %> prohibited this map from being saved:</h2>

      <ul>
      <% map.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <font color="white"><%= form.label :name, "Map Name" %></font>
    <%= form.text_field :name, class:"form-control", id: :map_name %>
  </div>

  <div class="field hide">
    <font color="white"><%= form.label :data %></font>
    <%= form.text_field :data, class:"form-control", id: :map_data %>
  </div>

  <div class="field">
    <font color="white"><%= form.label :story, "Story Name" %></font>
    <%= form.text_field :story, class:"form-control", id: :map_story %>
  </div>

  <div class="field">
    <font color="white"><%= form.label :adventure, "Adventure Category" %></font>
    <%= form.text_field :adventure, class:"form-control", id: :map_adventure %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
